# server

Placeholder. Not implemented.

This app is frontend-only for now — `client/` reads `data/vehicles.json` directly and simulates bidding entirely client-side (see the root `README.md` → Assumptions and Scope, and `guidelines/00-overview.md`). This directory is reserved for a real backend later, including the rival-bid simulation mentioned in the root README's "What I'd Do With More Time."
